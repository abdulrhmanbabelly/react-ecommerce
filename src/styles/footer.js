import { createUseStyles } from "react-jss";

let useFooterStyles = createUseStyles((theme) => ({
    footer : {
        backgroundColor: "#3f51b5",
        marginTop: "5vw !important",
    }
}));

export default useFooterStyles;